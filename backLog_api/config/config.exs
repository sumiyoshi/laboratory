use Mix.Config

# You can configure for your application as:
#
#     config :backLog_api, space_name: :value, api_key: :value
#
#     Application.get_env(:backLog_api, :key)
#

import_config "#{Mix.env}.exs"
