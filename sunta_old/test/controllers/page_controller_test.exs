defmodule Sunta.PageControllerTest do
  use Sunta.ConnCase

#  test "channel page", %{conn: conn} do
#    conn = get conn, page_path(conn, :channel)
#    assert html_response(conn, 200) =~ "チャンネル"
#  end

end
